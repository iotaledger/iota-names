name: Docker Build for IOTA Names Indexer

on:
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Checkout code
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@ac9db7bdf0b7668c1664ebdb4623d59cda7dbef6 # v2.0.1

      - name: Login to Private Docker Registry
        uses: docker/login-action@343f7c4344506aaaf2afa9a4d2cab89f0e50274d # v2.0.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Get the short commit hash
        run: |
          COMMIT_HASH=$(git rev-parse --short HEAD)
          echo "VERSION=$COMMIT_HASH" >> $GITHUB_ENV

      - name: Build and push Docker image
        uses: docker/build-push-action@2cdde995de11925a030ce8070c3e4cbbf5c41a78 # v4.0.0
        with:
          context: ./indexer
          file: ./indexer/docker/Dockerfile
          push: true
          tags: |
            ghcr.io/iotaledger/iota-names/iota-names-indexer:${{ env.VERSION }}
            ghcr.io/iotaledger/iota-names/iota-names-indexer:latest
          no-cache: true